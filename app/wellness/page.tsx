import { AppShell } from '@/components/app-shell';
import { WellnessClient } from '@/components/wellness-client';

export default function WellnessPage() {
  return (
    <AppShell>
      <WellnessClient />
    </AppShell>
  );
}
